<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.SurveyBoardQnA">

	<select id="surveyQuestionList" resultType="SurveyBoardQuestion">
		select * from
		survey_question
	</select>


	<!-- <select id="surveyList" parameterType="hashmap" resultType="SurveyBoardAnswer">
		<choose>
			<when test="sq_num == 1">
				select sa.sa_num, sa.sa_item
				from SURVEY_QUESTION sq,
				SURVEY_ANSWER sa
				WHERE sq.sq_num=sa.sq_num AND sq.sq_num=1;
			</when>
		</choose>
		<choose>
			<when test="sq_num == 2">
				select sa.sa_num, sa.sa_item
				from SURVEY_QUESTION sq,
				SURVEY_ANSWER sa
				WHERE sq.sq_num=sa.sq_num AND sq.sq_num=2;
			</when>
		</choose>
		<choose>
			<when test="sq_num == 3">
				select sa.sa_num, sa.sa_item
				from SURVEY_QUESTION sq,
				SURVEY_ANSWER sa
				WHERE sq.sq_num=sa.sq_num AND sq.sq_num=3;
			</when>
		</choose>
		<choose>
			<when test="sq_num == 4">
				select sa.sa_num, sa.sa_item
				from SURVEY_QUESTION sq,
				SURVEY_ANSWER sa
				WHERE sq.sq_num=sa.sq_num AND sq.sq_num=4;
			</when>
		</choose>
		<choose>
			<when test="sq_num == 5">
				select sa.sa_num, sa.sa_item
				from SURVEY_QUESTION sq,
				SURVEY_ANSWER sa
				WHERE sq.sq_num=sa.sq_num AND sq.sq_num=5;
			</when>
		</choose>
		<otherwise>

			select sa.sa_num, sa.sa_item
			from SURVEY_QUESTION sq, SURVEY_ANSWER sa
			WHERE sq.sq_num=sa.sq_num AND sq.sq_num=6;

		</otherwise>
	</select> -->
</mapper>